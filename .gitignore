node_modules/
.claude/
.data/
.env
*.env.local
dist/
coverage/
android/.gradle/
android/app/build/
android/build/
android/local.properties
*.log
~$*
.DS_Store
Thumbs.db
*.keystore
*.jks
android/keystore.properties

# temporários de sandbox / resultados de teste
.fuse_hidden*
test-results/
_pilot_*
*.aab

# IDE local (MCP/Cursor — não versionar)
.cursor/

# snapshots/backup locais da auditoria (tarballs pesados — nunca versionar)
.aud/

# Supabase CLI temp
supabase/.temp/

# Google Cloud service account keys (nunca versionar)
financaspro-*-*.json
*service-account*.json

/_to_delete/
/AUDITORIA_*.html
